# docker-compose.yml — Local development only
# Khởi chạy: docker compose up -d
# Dừng:      docker compose down
# Reset DB:  docker compose down -v && docker compose up -d
#
# Sao chép .env.example → .env trước khi chạy:
#   cp .env.example .env
#
# KHÔNG dùng file này cho Production.
# Production dùng biến môi trường trên Render/Vercel (xem docs/11-DEPLOYMENT.md mục 2).

services:
  postgres:
    image: postgres:16-alpine
    container_name: event_ticketing_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-event_ticketing_dev}
      POSTGRES_USER: ${POSTGRES_USER:-event_ticketing}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-event_ticketing_local_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-event_ticketing} -d ${POSTGRES_DB:-event_ticketing_dev}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: event_ticketing_backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # Datasource
      DATABASE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-event_ticketing_dev}
      DATABASE_URL_DIRECT: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-event_ticketing_dev}
      DATABASE_USERNAME: ${POSTGRES_USER:-event_ticketing}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:-event_ticketing_local_dev}

      # Flyway
      SPRING_FLYWAY_ENABLED: "true"

      # JWT
      JWT_SECRET: ${JWT_SECRET:-local-dev-jwt-secret-must-be-at-least-256-bits-long-for-hs256}
      JWT_ACCESS_TOKEN_EXPIRATION: ${JWT_ACCESS_TOKEN_EXPIRATION:-900000}
      JWT_REFRESH_TOKEN_EXPIRATION: ${JWT_REFRESH_TOKEN_EXPIRATION:-604800000}

      # Google Identity Services
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}

      # CORS — cho phép Vite dev server (localhost:5173)
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174}

      # Cookie — tắt Secure flag khi chạy local HTTP
      COOKIE_SECURE: ${COOKIE_SECURE:-false}

      # Spring profile (để trống → dùng config mặc định, không bật JSON logging)
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-}
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: event_ticketing_frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080/api/v1}
      VITE_WS_URL: ${VITE_WS_URL:-ws://localhost:8080/ws}
      VITE_GOOGLE_CLIENT_ID: ${VITE_GOOGLE_CLIENT_ID:-}
    ports:
      - "${FRONTEND_PORT:-5173}:5173"

volumes:
  pgdata:
    name: event_ticketing_pgdata
